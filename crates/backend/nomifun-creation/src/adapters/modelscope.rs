//! `modelscope` (魔搭：免费额度 / Lora / VL) adapter — **P1 stub**.
//!
//! Intentionally empty: reserves the extension seam so the P1 adapter drops in
//! here and registers via [`crate::default_adapters`] +
//! [`crate::adapters::route_adapter_id`] without touching the engine.
